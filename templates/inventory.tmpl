[master]
%{ for host in hosts ~}
%{ if host.master == true ~}
${ip_network}.${host.ip_octet}
%{ endif ~}
%{ endfor ~}

[worker]
%{ for host in hosts ~}
%{ if host.master != true ~}
${ip_network}.${host.ip_octet}
%{ endif ~}
%{ endfor ~}